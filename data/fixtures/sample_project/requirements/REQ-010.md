# REQ-010

## Title

Rate Limiting for Failed Authentication Attempts

## Description

The system shall enforce rate limiting on failed authentication attempts per email address. After a configurable number of consecutive failures (default: 5), further authentication attempts for that email shall be temporarily blocked. The lockout duration shall be configurable (default: 300 seconds).
